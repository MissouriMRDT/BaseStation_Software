<UserControl x:Class="RoverAttachmentManager.Views.Autonomy.AutonomyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <GroupBox Header="Controls" Margin="5" Grid.Column="0">
            <StackPanel>
                <Button x:Name="Enable" Margin="4" Padding="10">Enable</Button>
                <Button x:Name="AddWaypoint" Margin="4" Padding="10">Add Selected Waypoint</Button>
                <Button x:Name="ClearAllWaypoints" Margin="4" Padding="10">Clear All Waypoints</Button>
                <Button Click="Calibrate_Click" Margin="4" Padding="10">Calibrate</Button>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="State" Margin="5" Grid.Column="1">
            <StackPanel>
                <Button x:Name="StateIdle" Margin="4" Padding="10">Idle</Button>
                <Button x:Name="StateNavigate" Margin="4" Padding="10">Navigate</Button>
                <Button x:Name="StateSearch" Margin="4" Padding="10">Search</Button>
                <Button x:Name="StateApproachingMarker" Margin="4" Padding="10">Approaching Marker</Button>
                <Button x:Name="Shutdown" Margin="4" Padding="10">Shutdown</Button>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Map" Margin="5" Grid.Column="2">
            <StackPanel>
           
            </StackPanel>
        </GroupBox>

    </Grid>
    
    
    
    
</UserControl>
