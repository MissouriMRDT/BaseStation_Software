<UserControl x:Class="RoverAttachmentManager.Views.Science.SpectrometerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoverAttachmentManager.Views.Science"
             xmlns:cal="http://www.caliburnproject.org"
             xmlns:oxy="clr-namespace:OxyPlot.Wpf;assembly=OxyPlot.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <oxy:PlotView Margin="5" Height="150" Name="Plot" Model="{Binding SpectrometerPlotModel}"/>
        <UniformGrid Rows="1">
            <Label>Number of Runs:</Label>
            <TextBox x:Name="RunCount"/>
        </UniformGrid>
        <Button x:Name="DownloadSpectrometer">Run and Download</Button>
        <UniformGrid Rows="1">
            <Button cal:Message.Attach="SetUVLed(1)">Turn UV On</Button>
            <Button cal:Message.Attach="SetUVLed(0)">Turn UV Off</Button>
        </UniformGrid>
    </StackPanel>
</UserControl>
