<UserControl x:Class="RoverAttachmentManager.Views.Science.ScienceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cal="http://www.caliburnproject.org"
             mc:Ignorable="d" 
             d:DesignHeight="535" d:DesignWidth="500">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                    </Grid.RowDefinitions>
                    <Expander Grid.Row="0" Header="Actuation" IsExpanded="True" Margin="2.5">
                        <StackPanel>
                            <Label Grid.Row="2" Grid.Column="0">Rack Position</Label>
                            <UniformGrid Grid.Row="2" Grid.Column="1" Rows="1">
                                <Button cal:Message.Attach="SetScrewPosition(2)">Top</Button>
                                <Button cal:Message.Attach="SetScrewPosition(3)">Mid</Button>
                                <Button cal:Message.Attach="SetScrewPosition(4)">Ground</Button>
                            </UniformGrid>
                            <DockPanel>
                                <Label DockPanel.Dock="Left">Control State:</Label>
                                <Label DockPanel.Dock="Right" x:Name ="ControlState" HorizontalContentAlignment="Center" />
                            </DockPanel>
                        </StackPanel>
                    </Expander>
                    <Expander Grid.Row="1" Header="Spectrometer" IsExpanded="True" Margin="2.5">
                        <StackPanel>
                            <UniformGrid Rows="1">
                                <Label>Number of Runs: </Label>
                                <TextBox x:Name="RunCount"/>
                            </UniformGrid>
                            <UniformGrid Rows="1">
                                <Button x:Name="RequestSpectrometer">Request</Button>
                                <Button x:Name="DownloadSpectrometer">Download</Button>
                            </UniformGrid>
                            <UniformGrid>
                                <Button cal:Message.Attach="SetUVLed(1)">Turn UV On</Button>
                                <Button cal:Message.Attach="SetUVLed(0)">Turn UV Off</Button>
                            </UniformGrid>
                        </StackPanel>
                    </Expander>
                    <Expander Grid.Row="2" Header="Sensors" IsExpanded="True" Margin="2.5">
                        <StackPanel>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                    <RowDefinition />
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <Label Grid.Row="0" Grid.Column="0">Air Temperature</Label>
                                <Label Grid.Row="0" Grid.Column="1" x:Name="Sensor0Value" ContentStringFormat="{}{0:0.#&#176;}" />

                                <Label Grid.Row="1" Grid.Column="0">Air Humidity</Label>
                                <Label Grid.Row="1" Grid.Column="1" x:Name="Sensor1Value" ContentStringFormat="{}{0:0.\%;}" />

                                <Label Grid.Row="2" Grid.Column="0">Moisture</Label>
                                <Label Grid.Row="2" Grid.Column="1" x:Name="Sensor2Value" ContentStringFormat="{}{0:0.\%;}" />

                                <Label Grid.Row="3" Grid.Column="0">Soil Temperature</Label>
                                <Label Grid.Row="3" Grid.Column="1" x:Name="Sensor3Value" ContentStringFormat="{}{0:0.#&#176;}" />

                                <Label Grid.Row="4" Grid.Column="0">Methane</Label>
                                <Label Grid.Row="4" Grid.Column="1" x:Name="Sensor4Value" ContentStringFormat="{}{0:0.00;}" />
                            </Grid>

                            <UniformGrid Rows="1">
                                <Label>Save Data</Label>
                                <Button x:Name="SaveFileStart">Start</Button>
                                <Button x:Name="SaveFileStop">Stop</Button>
                            </UniformGrid>
                        </StackPanel>
                    </Expander>
                </Grid>
                
            </StackPanel>
            <StackPanel Grid.Column="1">
                
            </StackPanel>
        </Grid>
    </StackPanel>
</UserControl>
