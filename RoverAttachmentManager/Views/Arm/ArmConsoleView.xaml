<UserControl x:Class="RoverAttachmentManager.Views.Arm.ArmConsoleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RoverAttachmentManager.Views.Arm"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
        <StackPanel>
            <TextBox Name="ConsoleTextBox" Text="{Binding ConsoleText}" Margin="-4"
                 IsReadOnly="True" IsHitTestVisible="True" BorderThickness="0"
                 VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible"
                 TextWrapping="NoWrap" TextChanged="ConsoleText_Changed" />
        </StackPanel>
</UserControl>
