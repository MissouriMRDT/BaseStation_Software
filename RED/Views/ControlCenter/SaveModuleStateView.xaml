<mui:ModernDialog x:Class="RED.Views.ControlCenter.SaveModuleStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="Save Module State" mc:Ignorable="d" >
    <Grid Width="350" FocusManager.FocusedElement="{Binding ElementName=NameTextBox}">
        <StackPanel>
            <mui:BBCodeBlock BBCode="[b]New Saves[/b]: Enter a [i]unique[/i] name." />
            <mui:BBCodeBlock BBCode="[b]Overwrite[/b]: Enter an [i]existing[/i] name." Margin="0 0 0 10" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Name="NameTextBox"></TextBox>
        </StackPanel>
    </Grid>
</mui:ModernDialog>
