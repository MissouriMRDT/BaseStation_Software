<mui:ModernDialog x:Class="RED.Views.ControlCenter.RemoveModuleStateView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:mui="http://firstfloorsoftware.com/ModernUI"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        Title="Remove Module State" mc:Ignorable="d">
    <Grid Width="350">
        <StackPanel>
            <mui:BBCodeBlock BBCode="Choose an existing state to remove." Margin="0 0 0 10" />
            <ComboBox SelectedItem="{Binding SelectedItem}" ItemsSource="{Binding SavedModuleStates, UpdateSourceTrigger=PropertyChanged}" Width="150" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</mui:ModernDialog>

