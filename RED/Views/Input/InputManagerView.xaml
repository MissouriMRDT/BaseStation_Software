<UserControl x:Class="RED.Views.Input.InputManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Center">
        <Label>Control Scheme:</Label>
        <ComboBox x:Name="Mappings" SelectedItem="{Binding SelectedMapping}" SelectionChanged="Mappings_SelectionChanged">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Foreground="Gray">&#160;[</TextBlock>
                        <TextBlock Foreground="Gray" Text="{Binding DeviceType}" />
                        <TextBlock Foreground="Gray"> &#x2192; </TextBlock>
                        <TextBlock Foreground="Gray" Text="{Binding ModeType}" />
                        <TextBlock Foreground="Gray">]</TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </StackPanel>
</UserControl>
