<UserControl x:Class="RED.Views.Settings.Input.Controllers.XboxControllerInputSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Width="400" HorizontalAlignment="Left">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Content="{Binding Index}" ContentStringFormat="#." />
        <CheckBox Grid.Column="1" x:Name="AutoDeadzone">AutoDeadzone</CheckBox>
        <Label Grid.Column="2" Margin="50 0 0 0">Manual Deadzone (0-32768)</Label>
        <TextBox Grid.Column="3" x:Name="ManualDeadzone" />
    </Grid>
</UserControl>
