<UserControl x:Class="RED.Views.Settings.Modules.ScienceSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Width="300" HorizontalAlignment="Left">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0">CCD IP Address</Label>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="CCDIPAddress" />
            <Label Grid.Row="1" Grid.Column="0">CCD Port Number</Label>
            <TextBox Grid.Row="1" Grid.Column="1" x:Name="CCDPortNumber" />
            <Label Grid.Row="2" Grid.Column="0">CCD File Path</Label>
            <TextBox Grid.Row="2" Grid.Column="1" x:Name="CCDFilePath" />
        </Grid>
    </StackPanel>
</UserControl>
