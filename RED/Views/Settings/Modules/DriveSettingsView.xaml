<UserControl x:Class="RED.Views.Settings.Modules.DriveSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Width="300" HorizontalAlignment="Left">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0">Speed Limit (0 - 1000)</Label>
            <TextBox Grid.Row="0" Grid.Column="1" x:Name="SpeedLimit" />
        </Grid>
        <CheckBox x:Name="UseLegacyDataIds">Use Legacy DataIds</CheckBox>
    </StackPanel>
</UserControl>
