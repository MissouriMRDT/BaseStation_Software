<UserControl x:Class="RED.Views.Modules.CameraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TextBox x:Name="Mux1Index" Grid.Column="0" Grid.Row="0" />
        <Button x:Name="SetMux1" Grid.Column="1" Grid.Row="0">Set Mux 1</Button>
        <TextBox x:Name="Mux2Index" Grid.Column="0" Grid.Row="1" />
        <Button x:Name="SetMux2" Grid.Column="1" Grid.Row="1">Set Mux 2</Button>
    </Grid>
</UserControl>
