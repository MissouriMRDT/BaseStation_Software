<UserControl x:Class="RED.Views.Modules.LightingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <ToggleButton x:Name="HeadlightsEnabled">Headlights Enabled</ToggleButton>
        <ToggleButton x:Name="Enabled">Underglow Enabled</ToggleButton>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Grid.Row="0" Grid.Column="0">Red</Label>
            <Slider Grid.Row="0" Grid.Column="1" x:Name="Red" Minimum="0" Maximum="255" />
            <Label Grid.Row="1" Grid.Column="0">Green</Label>
            <Slider Grid.Row="1" Grid.Column="1" x:Name="Green" Minimum="0" Maximum="255" />
            <Label Grid.Row="2" Grid.Column="0">Blue</Label>
            <Slider Grid.Row="2" Grid.Column="1" x:Name="Blue" Minimum="0" Maximum="255" />
        </Grid>
        
    </StackPanel>
</UserControl>
