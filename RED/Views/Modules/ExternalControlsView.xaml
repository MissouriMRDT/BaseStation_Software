<UserControl x:Class="RED.Views.Modules.ExternalControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label Grid.Column="0" Grid.Row="0">Ext Ctrls Reset</Label>
        <DockPanel Grid.Column="1" Grid.Row="0">
            <Button DockPanel.Dock="Right" x:Name="ExternalControlsReset">Reset</Button>
            <TextBox x:Name="ResetCode" />
        </DockPanel>
    </Grid>
</UserControl>
